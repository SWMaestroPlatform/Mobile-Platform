<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="soma.iot.sympathyhome" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >

            <!-- android:theme="@android:style/Theme.Holo.Light.NoActionBar" > -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.FamilyPhotoActivity"
            android:label="@string/title_activity_family_photo" >
        </activity>
        <activity
            android:name=".activity.SmartHomeAcitivy"
            android:label="@string/title_activity_smart_home_acitivy" >
        </activity>
        <activity
            android:name=".activity.ReminderActivity"
            android:label="@string/title_activity_reminder" >
        </activity>
        <activity
            android:name=".activity.SmartThingsActivity"
            android:label="@string/title_activity_smart_things" >
        </activity>
    </application>

</manifest>
